export class courseService {
    // TODO: async createCourse()
    // TODO: async getCourseById(courseId: string)
    // TODO: async updateCourse(courseId: string, updatedDate: Date)
    // TODO: async deleteCourse(courseId: string)
    // TODO: async getCourseStudents(courseId: string)
    // TODO: async getCourseEnrollments(courseId: string)
}