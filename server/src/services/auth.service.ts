export class AuthService {
	// TODO: async Login(username: string, password: string): Promise<User>
	// TODO: async Register(user: User): Promise<User>
	// TODO: async RefreshToken(refreshToken: string): Promise<Tokens>
	// TODO: async Logout(userId: string): Promise<void>
}
